#include "base.hpp"

enum Fruits {
    APPLE = 1,
    ORANGE,
    BANANA,
    LEMON
};

enum class MathematicalFruits {
    TWO_APPLES = 2*APPLE,
    LEMON_PLUS_ORANGE  = LEMON + ORANGE
};

enum FromBase {
    ONE_PLUS_TWO = ONE + TWO
};
